import 'package:flutter_test/flutter_test.dart';

void main() {
  test('adds one to input values', () {
    // final deviceEmulator = DeviceEmulator(
    //   builder: (context) {
    //     return Container(
    //       color: Colors.red,
    //     );
    //   },
    // );
    // expect(deviceEmulator.addOne(2), 3);
  });
}
